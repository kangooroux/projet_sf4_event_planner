{% for event in event_list %}

    <div class="column is-one-third">
        <div class="box">
            <h1 class="subtitle"><a href="#">{{ event.name }}</a></h1>
            <p>Date: {{ event.date|date('d/m/Y') }}</p>
            <p>Lieu: {{ event.location }}</p>
            <br>
            <p>{{ event.description }}</p>
        </div>
    </div>

{% endfor %}